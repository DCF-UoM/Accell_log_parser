# Accell_log_parser
python script for parsing accellerator setup logs in pdf format

If it does not parse your file correctly, check that the headings, parameters or units are included in the right list at the top of the file.  If these are not declared it is just guessing so try declaring them here.

Requirements:
- python 3
- pandas
- pypdf
